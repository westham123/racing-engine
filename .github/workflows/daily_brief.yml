name: Racing Engine — Daily Brief & Updates [DISABLED]

# DISABLED 29 Apr 2026 — all email sending now handled by Perplexity crons
# Replaced by: 3fb7f776 (operator), 4eac6ab1 (morning), a54556fb (confirmed),
#              909fe390 (late alerts), 385f97ff (evening summary)
# This workflow was sending stale ghost emails daily — do not re-enable.

on:
  # All schedules removed — workflow_dispatch kept for emergency manual use only
  workflow_dispatch:
    inputs:
      brief_type:
        description: 'Manual trigger only — all auto-sends disabled'
        required: false
        default: 'test'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "This GitHub Actions workflow has been disabled as of 29 Apr 2026."
          echo "All email sending is now handled by Perplexity scheduled tasks."
          echo "Do not re-enable without checking with the operator first."
          exit 0
